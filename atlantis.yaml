version: 3
automerge: true
projects:
# - name: aws
#   dir: cloud/aws
#   workspace: default
#   terraform_version: v0.12.0
#   apply_requirements: [mergeable, approved]
#   workflow: empty
- name: gcp
  dir: cloud/gcp
  workspace: default
  terraform_version: v0.12.0
  apply_requirements: [mergeable, approved]
  workflow: gcponly
# workflows:
#   empty: {}
#   myworkflow:
#     plan:
#       steps:
#       - run: echo planning
#     apply:
#       steps:
#       - run: echo applying
